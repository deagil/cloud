/** Port for sandbox-agent HTTP server inside the Vercel sandbox. */
export const SANDBOX_AGENT_PORT = 2468

/** @sandbox-agent/cli version pinned for reproducible sandboxes. */
export const SANDBOX_AGENT_CLI_VERSION = '0.4.2'
